import { ReactNode } from 'react';

export type CardType = {
  title: string;
  children?: ReactNode | undefined;
};

export type InputType = {
  title: string;
  children?: ReactNode | undefined;
};
